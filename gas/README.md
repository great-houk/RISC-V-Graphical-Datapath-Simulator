This is a build of [GNU binutils](https://www.gnu.org/software/binutils) as assembler (also known as gas) into WASM using
[emscripten](https://emscripten.org/).

You can run `build-gas.sh` to build it from scratch.
